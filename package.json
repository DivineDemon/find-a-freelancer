{
	"devDependencies": {
		"husky": "^9.1.7"
	},
	"scripts": {
		"prepare": "husky",
		"lint:frontend": "cd frontend && pnpm run lint",
		"lint:backend": "cd backend && ruff check . --fix",
		"lint:all": "pnpm run lint:backend && pnpm run lint:frontend",
		"format:frontend": "cd frontend && pnpm run lint",
		"format:backend": "cd backend && ruff format .",
		"format:all": "pnpm run format:backend && pnpm run format:frontend",
		"dev:frontend": "cd frontend && pnpm run dev",
		"dev:backend": "cd backend && uvicorn app.main:app --reload",
		"install:all": "pnpm install && cd frontend && pnpm install && cd ../backend && python3 -m pip install -r requirements.txt"
	}
}
